If you have bytes like:

10 2A       ; LDA #$2A
13 00 FF    ; STA $FF00
FF          ; HLT


Create a file program.bin:

macOS/Linux using printf:

printf '\x10\x2A\x13\x00\xFF\xFF' > program.bin


(that last \xFF is HLT)

Or from hex text:

cat > program.hex <<'EOF'
10 2A 13 00 FF FF
EOF
xxd -r -p program.hex > program.bin


> reset
> loadhex programs/blink.hex 0000
> setrv 0000
> reset
> d 0000 8
> g
> m ff00 1

To run the virtual CPU: ./run_cli.sh
To view commands: type 'help'


I will add more user friendly instructions on how to utilize the Virtual CPU for maximum benefits.
